# Core display
Pillow>=10.0
numpy>=1.24
spidev>=3.6
RPi.GPIO>=0.7

# GPS
gpxpy>=1.6
# gpsd client: install from apt or pip
# sudo apt install python3-gps
# or: pip install gpsd-py3

# Map tiles
mapbox-vector-tile>=2.0

# Bluetooth (BlueZ over D-Bus): pairing, plus BLE heart rate and cadence
dbus-fast>=2.21

# Strava upload and the Spotify Web API
aiohttp>=3.9

# Music metadata for the local library (falls back to filenames without it)
mutagen>=1.47

# Optional: faster route GPX parsing
lxml>=5.0
